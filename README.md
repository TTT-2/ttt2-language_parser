# TTT2 Language parser

A python tool that parses the language files and cleans them up

Run: `clear ; python parse.py --in "../TTT2/lua/terrortown/lang" --out "../TTT2/lua/terrortown/lang" --base "en"`
